import{effectiveEntitlement,subscriptionRetainsAccess,usageLevel}from'./entitlementModel';
describe('billing entitlement rules',()=>{const now=new Date('2026-08-08T12:00:00Z');test('active and unexpired trial access remain available',()=>{expect(effectiveEntitlement({status:'trial',endsAt:'2026-08-12T12:00:00Z'},now)).toMatchObject({effectiveStatus:'trial',daysRemaining:4});expect(subscriptionRetainsAccess({status:'past_due'},now)).toBe(true)});test('expired trial is locked without deleting its record',()=>{const trial={id:'trial-1',status:'trial',endsAt:'2026-08-07T12:00:00Z'};expect(effectiveEntitlement(trial,now)).toMatchObject({id:'trial-1',effectiveStatus:'expired',daysRemaining:0})});test('canceled access lasts through the paid period',()=>{expect(subscriptionRetainsAccess({status:'canceled',current_period_end:'2026-08-09T12:00:00Z'},now)).toBe(true);expect(subscriptionRetainsAccess({status:'canceled',current_period_end:'2026-08-07T12:00:00Z'},now)).toBe(false)});test('AI usage thresholds are explicit',()=>{expect(usageLevel(79,100)).toBe('normal');expect(usageLevel(80,100)).toBe('warning');expect(usageLevel(100,100)).toBe('limit')})});
