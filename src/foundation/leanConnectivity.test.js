import{evidenceGraph,findingCandidate,normalizeObjectLink}from'./leanConnectivity';
test('canonical graph preserves identities without copying payloads',()=>{const objects=[{id:'v1',type:'value-stream-map',title:'Current VSM',payload:{large:'not copied'}},{id:'a3',type:'a3',title:'A3'}],link={id:'l1',fromId:'v1',fromType:'value-stream-map',toId:'a3',toType:'a3',relationship:'evidence_for'},graph=evidenceGraph(objects,[link]);expect(graph.nodes.map(x=>x.id)).toEqual(['v1','a3']);expect(graph.nodes[0]).not.toHaveProperty('payload');expect(graph.edges[0].relationship).toBe('evidence_for')});
test('malformed and legacy links normalize to restricted placeholders',()=>{expect(normalizeObjectLink({fromId:'missing',fromType:'legacy',toId:'deleted',toType:'unknown',relationship:'made_up'},[])).toMatchObject({relationship:'related_to',from:{status:'Unavailable'},to:{title:'Unavailable or restricted object'}})});
test('existing deterministic Daily KPI miss produces full finding provenance',()=>{const record={id:'daily-1',type:'daily-management',revision:4},candidate=findingCandidate(record,{items:[{id:'k1',name:'Delivery',category:'Delivery',direction:'higher',target:100,actual:91}]});expect(candidate).toMatchObject({findingType:'daily_kpi_miss',sourceArtifactId:'daily-1',sourceElementId:'k1',sourceRevision:4,observedValue:91,threshold:100,evidence:{artifactId:'daily-1',revision:4,elementId:'k1'}})});
