import{getSuite}from'../config/suites';import{supabase}from'../lib/supabase';import{validateProjectOwnership}from'../services/persistenceSafety';import{TOLLGATE_TYPE,isTollgate}from'../foundation/tollgate';
const SUITE=getSuite('operational-excellence').id,requireCloud=()=>{if(!supabase)throw new Error('cloud-not-configured');return supabase};
const assertScope=async(client,value)=>{const{data:{user}}=await client.auth.getUser();const{data:project,error}=await client.from('projects').select('id,organization_id').eq('id',value.project_id).maybeSingle();if(error)throw error;validateProjectOwnership({userId:user?.id,organizationId:value.organization_id,projectId:value.project_id,createdBy:value.created_by,project})};
const mark=content=>({...content,item_type:TOLLGATE_TYPE});
export const tollgateRepository={async list(projectId){const{data,error}=await requireCloud().from('approvals').select('*').eq('project_id',projectId).eq('suite',SUITE).eq('content->>item_type',TOLLGATE_TYPE).order('created_at',{ascending:false});if(error)throw error;return data},async create(record){const client=requireCloud(),value={...record,suite:SUITE,content:mark(record.content)};await assertScope(client,value);const{data,error}=await client.from('approvals').insert(value).select().single();if(error)throw error;return data},async update(record){if(!record.id)throw new Error('tollgate-update-requires-id');const client=requireCloud(),value={...record,suite:SUITE,content:mark(record.content)},version=(Number(record.version)||1)+1;await assertScope(client,value);let query=client.from('approvals').update({...value,version}).eq('id',record.id).eq('suite',SUITE).eq('content->>item_type',TOLLGATE_TYPE);if(record.version!=null)query=query.eq('version',record.version);const{data,error}=await query.select().single();if(error)throw error;if(!isTollgate(data))throw new Error('tollgate-not-found');return data},async organizationRole(organizationId,userId){const{data,error}=await requireCloud().from('organization_memberships').select('role').eq('organization_id',organizationId).eq('user_id',userId).maybeSingle();if(error)throw error;return data?.role||''}};
