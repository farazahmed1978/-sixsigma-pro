import React,{useEffect,useMemo,useState}from'react';import{useNavigate}from'react-router-dom';import{useAuth}from'../context/AuthContext';import{useInteractions}from'../context/InteractionContext';import{useReport}from'../context/ReportContext';import{tollgateRepository}from'../repositories/tollgateRepository';import{TOLLGATE_PHASES,TOLLGATE_STATUSES,appendTollgateEvent,canReviewTollgate,evaluateTollgateReadiness,phaseAfterDecision,tollgateDetail,tollgateReportItem}from'../foundation/tollgate';import'./ProjectTollgates.css';import'./ProjectTollgatesStates.css';
const activeStatus=status=>[TOLLGATE_STATUSES.SUBMITTED,TOLLGATE_STATUSES.IN_REVIEW].includes(status),latestFor=(reviews,phase)=>reviews.filter(row=>tollgateDetail(row).phase===phase).sort((a,b)=>(tollgateDetail(b).attempt||1)-(tollgateDetail(a).attempt||1))[0];
export default function ProjectTollgates({project,documents=[],analyses=[],evidence=[],datasets=[],correctiveActions=[],reviews=[],onReviewsChange,updateProject,onOpenBinder,requestedPhase=''}){const{user,profile}=useAuth(),{toast}=useInteractions(),report=useReport(),[phase,setPhase]=useState(requestedPhase||project.currentPhase||'Define'),[loading,setLoading]=useState(!reviews.length),[error,setError]=useState(''),[role,setRole]=useState(''),[reviewerKey,setReviewerKey]=useState(''),[comments,setComments]=useState(''),[saving,setSaving]=useState(false);const context={organizationId:project.organizationId||profile?.default_organization_id,projectId:project.id};
 useEffect(()=>{let active=true;Promise.all([tollgateRepository.list(project.id),tollgateRepository.organizationRole(context.organizationId,user?.id)]).then(([rows,nextRole])=>{if(active){onReviewsChange(rows);setRole(nextRole)}}).catch(next=>active&&setError(next.message||'Tollgates could not be loaded.')).finally(()=>active&&setLoading(false));return()=>{active=false}},[project.id,context.organizationId,user?.id,onReviewsChange]);
 useEffect(()=>{if(requestedPhase)setPhase(requestedPhase)},[requestedPhase]);
 const resources=useMemo(()=>({documents,analyses,evidence,datasets,correctiveActions}),[documents,analyses,evidence,datasets,correctiveActions]),readiness=evaluateTollgateReadiness(project,phase,resources),latest=latestFor(reviews,phase),gate=latest&&tollgateDetail(latest),attempts=reviews.filter(row=>tollgateDetail(row).phase===phase).sort((a,b)=>(tollgateDetail(b).attempt||1)-(tollgateDetail(a).attempt||1)),team=(project.team||[]).filter(member=>(member.email||member.userId)&&member.userId!==user?.id&&String(member.email||'').toLowerCase()!==String(user?.email||'').toLowerCase()),reviewer=team.find(member=>(member.userId||member.email)===reviewerKey),authorized=gate&&canReviewTollgate({user,organizationRole:role,review:gate});
 useEffect(()=>{setReviewerKey(gate?.assignedReviewerId||gate?.assignedReviewerEmail||'')},[gate?.id,gate?.assignedReviewerId,gate?.assignedReviewerEmail]);
 const submit=async()=>{if(!readiness.readyToSubmit||saving)return;if(!reviewer){setError('Assign a project-team reviewer before submission.');return}setSaving(true);setError('');try{const attempt=(attempts[0]?tollgateDetail(attempts[0]).attempt:0)+1,at=new Date().toISOString(),content={phase,attempt,submittedBy:user.id,submittedByName:user.user_metadata?.full_name||user.email,submittedAt:at,assignedReviewerId:reviewer.userId||'',assignedReviewerName:reviewer.name||reviewer.email,assignedReviewerEmail:reviewer.email||'',readiness,evidenceSnapshot:readiness.evidenceSnapshot,reviewComments:'',decision:'',decisionBy:'',decisionByName:'',decisionAt:'',returnReason:'',events:[{id:crypto.randomUUID(),type:attempt>1?'resubmitted':'submitted',at,actorId:user.id,actorName:user.email,comments:comments.trim()}]};const saved=await tollgateRepository.create({project_id:project.id,organization_id:context.organizationId,created_by:user.id,title:`${phase} Tollgate — Attempt ${attempt}`,status:TOLLGATE_STATUSES.SUBMITTED,methodology:'lean-six-sigma',lifecycle_phase:phase,content});onReviewsChange([saved,...reviews]);setComments('');toast(`${phase} gate submitted for review.`)}catch(next){setError(next.message||'The tollgate could not be submitted.')}finally{setSaving(false)}};
 const decide=async status=>{if(!authorized||saving)return;const note=comments.trim();if([TOLLGATE_STATUSES.RETURNED,TOLLGATE_STATUSES.CONDITIONAL,TOLLGATE_STATUSES.REJECTED].includes(status)&&!note){setError('Comments are required for this decision.');return}setSaving(true);setError('');try{const at=new Date().toISOString(),prior=tollgateDetail(latest),eventType=status===TOLLGATE_STATUSES.IN_REVIEW?'review-started':status===TOLLGATE_STATUSES.RETURNED?'returned-for-revision':'decision',next=appendTollgateEvent(prior,{type:eventType,at,actorId:user.id,actorName:user.user_metadata?.full_name||user.email,status,comments:note}),content={...latest.content,...next,reviewComments:note||prior.reviewComments,decision:status===TOLLGATE_STATUSES.IN_REVIEW?'':status,decisionBy:status===TOLLGATE_STATUSES.IN_REVIEW?'':user.id,decisionByName:status===TOLLGATE_STATUSES.IN_REVIEW?'':user.user_metadata?.full_name||user.email,decisionAt:status===TOLLGATE_STATUSES.IN_REVIEW?'':at,returnReason:status===TOLLGATE_STATUSES.RETURNED?note:prior.returnReason};const{created_by,created_at,updated_at,...existing}=latest,saved=await tollgateRepository.update({...existing,status,content});onReviewsChange(reviews.map(row=>row.id===saved.id?saved:row));if([TOLLGATE_STATUSES.APPROVED,TOLLGATE_STATUSES.CONDITIONAL].includes(status)){const nextPhase=phaseAfterDecision(phase,status);if(nextPhase!==project.currentPhase)await updateProject(project.id,{currentPhase:nextPhase,activityLog:[{id:`tollgate-${saved.id}`,action:`${phase} tollgate ${status.toLowerCase()}`,assetType:'tollgate',assetId:saved.id,at},...(project.activityLog||[])]})}setComments('');toast(`${phase} gate marked ${status.toLowerCase()}.`)}catch(next){setError(next.message||'The review decision could not be saved.')}finally{setSaving(false)}};
 const addReport=async()=>{if(!latest)return;await report.addReportItem(tollgateReportItem(latest,project));toast('Tollgate summary added to the report.')};
 const addComment=async()=>{const note=comments.trim();if(!authorized||!note||saving)return;setSaving(true);setError('');try{const prior=tollgateDetail(latest),next=appendTollgateEvent(prior,{type:'comment',actorId:user.id,actorName:user.user_metadata?.full_name||user.email,comments:note}),{created_by,created_at,updated_at,...existing}=latest,saved=await tollgateRepository.update({...existing,content:{...latest.content,...next,reviewComments:note}});onReviewsChange(reviews.map(row=>row.id===saved.id?saved:row));setComments('');toast('Review comment recorded.')}catch(next){setError(next.message||'The review comment could not be saved.')}finally{setSaving(false)}};
   return <section className="oe-tollgates"><header><div><span>DMAIC GOVERNANCE</span><h2>Phase Tollgates</h2><p>Submit evidence-backed phases for accountable review without locking historical work.</p></div><button type="button" className="btn-secondary" onClick={onOpenBinder}>Open Project Binder</button></header>{error&&<div className="oe-tollgate-error" role="alert">{error}</div>}{loading?<p role="status">Loading tollgates…</p>:<><nav aria-label="DMAIC tollgates">{TOLLGATE_PHASES.map(item=>{const itemGate=latestFor(reviews,item),status=itemGate?tollgateDetail(itemGate).status:'Not Submitted';return <button type="button" className={phase===item?'active':''} key={item} onClick={()=>{setPhase(item);setComments('')}}><b>{item}</b><small>{status}</small></button>})}</nav><div className="oe-tollgate-layout"><section className="oe-tollgate-readiness"><header><div><span>{phase.toUpperCase()} GATE</span><h3>Submission readiness</h3></div><strong className={readiness.readyToSubmit?'ready':'blocked'}>{readiness.readyToSubmit?'Ready':'Blocked'}</strong></header><RequirementList title="Blockers" items={readiness.blockers}/><RequirementList title="Warnings" items={readiness.warnings}/>{readiness.completedRequirements.length>0&&<details><summary>{readiness.completedRequirements.length} requirements complete</summary><ul>{readiness.completedRequirements.map(item=><li key={item.code}>{item.label}</li>)}</ul></details>}<label>Reviewer<select value={reviewerKey} disabled={Boolean(gate&&activeStatus(gate.status))} onChange={event=>setReviewerKey(event.target.value)}><option value="">Assign reviewer</option>{team.map(member=><option key={member.userId||member.email} value={member.userId||member.email}>{member.name||member.email} · {member.role||'Team member'}</option>)}</select></label><label>Submission note<textarea value={comments} onChange={event=>setComments(event.target.value)} placeholder="Context for the reviewer"/></label><button type="button" className="btn-primary" disabled={!readiness.readyToSubmit||saving||Boolean(gate&&activeStatus(gate.status))} onClick={submit}>{gate?.status===TOLLGATE_STATUSES.RETURNED?'Resubmit Phase':'Submit Phase'}</button></section><section className="oe-tollgate-review">{gate?<><header><div><span>ATTEMPT {gate.attempt||1}</span><h3>{gate.status}</h3></div><small>{gate.assignedReviewerName||'Unassigned reviewer'}</small></header><dl><div><dt>Submitted</dt><dd>{new Date(gate.submittedAt).toLocaleString()} by {gate.submittedByName}</dd></div><div><dt>Decision</dt><dd>{gate.decision||'Pending'}{gate.decisionAt&&` · ${new Date(gate.decisionAt).toLocaleString()}`}</dd></div></dl>{authorized&&activeStatus(gate.status)&&<><label>Review comments<textarea value={comments} onChange={event=>setComments(event.target.value)} placeholder="Record review findings, conditions, or revision instructions"/></label><div className="oe-tollgate-actions"><button className="btn-secondary" disabled={!comments.trim()} onClick={addComment}>Add Comment</button>{gate.status===TOLLGATE_STATUSES.SUBMITTED&&<button className="btn-secondary" onClick={()=>decide(TOLLGATE_STATUSES.IN_REVIEW)}>Start Review</button>}<button className="btn-secondary" onClick={()=>decide(TOLLGATE_STATUSES.RETURNED)}>Return for Revision</button><button className="btn-secondary" onClick={()=>decide(TOLLGATE_STATUSES.CONDITIONAL)}>Conditionally Approve</button><button className="btn-primary" onClick={()=>decide(TOLLGATE_STATUSES.APPROVED)}>Approve</button><button className="btn-ghost danger" onClick={()=>decide(TOLLGATE_STATUSES.REJECTED)}>Reject</button></div></>} {!authorized&&activeStatus(gate.status)&&<p className="oe-tollgate-notice">Decision controls are limited to the assigned reviewer or an organization owner/admin. Submitters cannot approve their own gate.</p>}<button className="btn-secondary" type="button" onClick={addReport}>Add summary to Report</button><h4>Review history</h4><ol className="oe-tollgate-history">{[...(gate.events||[])].reverse().map(event=><li key={event.id}><b>{String(event.type||'event').replace(/-/g,' ')}</b><span>{event.actorName} · {new Date(event.at).toLocaleString()}</span>{event.comments&&<p>{event.comments}</p>}</li>)}</ol>{attempts.length>1&&<small>{attempts.length} durable submission attempts retained.</small>}</>:<div className="oe-tollgate-empty"><h3>Not submitted</h3><p>Resolve hard blockers, assign a reviewer, and submit this phase.</p></div>}</section></div></>}</section>}
function RequirementList({title,items}){const navigate=useNavigate();if(!items.length)return null;return <div className={`oe-tollgate-requirements ${title.toLowerCase()}`}><h4>{title}</h4><ul>{items.map(item=><li key={item.code}><span>{item.label}</span><span className="oe-tollgate-requirement-actions">{item.actions.map(next=><button type="button" className="btn-ghost" key={`${item.code}-${next.destination}`} onClick={()=>navigate(next.destination)}>{next.actionLabel}</button>)}</span></li>)}</ul></div>}
