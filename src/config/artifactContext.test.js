import {artifactContextDecision,artifactDefinition,existingProjectArtifact,matchingSharedFields,saveStandaloneArtifact,getStandaloneArtifact} from './artifactContext';
const template={id:'business-case',name:'Business Case',phase:'Define',sections:[{fields:[{id:'executiveSummary',label:'Executive Summary'},{id:'risks',label:'Risks'}]}]};
test('registry resolves shared advisories and explicit project or standalone ownership',()=>{expect(matchingSharedFields(template,{businessCaseSummary:'Existing rationale'})).toEqual([expect.objectContaining({sharedKey:'businessCaseSummary',targetField:'executiveSummary',source:'Project Charter'})]);expect(artifactContextDecision({mode:'project',projectId:'p'}).projectId).toBe('p');expect(artifactContextDecision({mode:'standalone',projectId:'p'}).projectId).toBe('')});
test('documents are singleton while analyses remain repeatable',()=>{expect(artifactDefinition({kind:'document',template}).cardinality.cardinality).toBe('singleton');expect(artifactDefinition({kind:'analysis',template:{id:'anova',name:'ANOVA'}}).cardinality.cardinality).toBe('repeatable');expect(existingProjectArtifact({definition:artifactDefinition({kind:'document',template}),project:{documents:{'document-business-case':{id:'existing'}}}}).id).toBe('existing');expect(existingProjectArtifact({definition:artifactDefinition({kind:'analysis',template:{id:'anova'}}),project:{}})).toBeNull()});
test('standalone context persists without project ownership',()=>{const record={id:'standalone-test',projectId:'',artifactContext:{mode:'standalone'}};saveStandaloneArtifact(record);expect(getStandaloneArtifact(record.id)).toEqual(record)});
test('shared advisory does not inspect unrelated or cross-project data',()=>{expect(matchingSharedFields(template,{goalSummary:'Other project value'})).toEqual([])});
