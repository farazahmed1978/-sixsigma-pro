import{supabase}from'../lib/supabase';
export async function registerSuiteInterest({email,suiteKey,source='suite-overview'}){const{data}=supabase?await supabase.auth.getSession():{data:{}};const response=await fetch('/api/suites/register-interest',{method:'POST',headers:{'Content-Type':'application/json',...(data?.session?.access_token?{Authorization:`Bearer ${data.session.access_token}`}:{})},body:JSON.stringify({email,suiteKey,source})});const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.error||'We could not add you to the list. Please try again.');return body}
